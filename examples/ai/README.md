# AI example evidence status

`ComponentList.SC2Components`, the `AIData` root and `Definition Id` are Editor-output-confirmed.

The populated `Wave/CreateUnits/Unit` example demonstrates the gated theoretical writer. Its exact nested serialization is **UNTESTED in SC2Editor** and must not be described as L5-valid. Run differential Editor discovery, then update `generated/ai-module-schema.json` before removing the `allowUnconfirmedStructure` gate.
